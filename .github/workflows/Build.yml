name: Build project
on: 
  push:
    branches:
      - main
      
env:
  BUILD_DIR: build/
  FILE_NAME: Build-
  RELEASE_NAME: release-${{ github.sha }} 

jobs:
  buildForAllSupportedPlatforms:
    name: Build for ${{ matrix.targetPlatform }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        targetPlatform: [StandaloneWindows64]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true
          
      - uses: actions/cache@v3
        with:
          path: Library
          key: Library-${{ matrix.targetPlatform }}
          restore-keys: Library-
          
      - if: matrix.targetPlatform == 'Android'
        uses: jlumbroso/free-disk-space@v1.3.1
        
      - uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: ${{ matrix.targetPlatform }}

      - uses: actions/upload-artifact@v4
        id: upload-step
        with:
          name: ${{ env.FILE_NAME }}${{ matrix.targetPlatform }}
          path: ${{ env.BUILD_DIR }}${{ matrix.targetPlatform }}

  upload_files:
        name: Upload ${{ matrix.targetPlatform }} zip to Release
        runs-on: ubuntu-latest
        permissions: write-all
        needs: [buildForAllSupportedPlatforms] # Ensures this job runs after the build
        strategy:
          fail-fast: false
          matrix:
            targetPlatform: [StandaloneWindows64]
        steps:
          - name: Download Build Artifact
            uses: actions/download-artifact@v4
            id: download-step
            with:
              artifact-ids: ${{ needs.steps.upload-step.outputs.artifact-id }}
              path: ./download
            
          - name: Zip Build
            run: |
              cd download
              zip -r ../${{ env.FILE_NAME }}${{ matrix.targetPlatform }}.zip .
            
          - name: Upload to GitHub Release
            id: release
            uses: softprops/action-gh-release@v1
            with:
              tag_name: ${{ env.RELEASE_NAME }}
            env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              
          - name: Upload Release Asset
            id: upload-release-asset 
            uses: actions/upload-release-asset@v1
            env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            with:
              upload_url: ${{ steps.release.outputs.upload_url }}
              asset_path: ./${{ env.FILE_NAME }}${{ matrix.targetPlatform }}.zip
              asset_name: ${{ env.FILE_NAME }}${{ matrix.targetPlatform }}.zip
              asset_content_type: application/zip
              
  generate_email_body:
    name: Generate Email Body
    runs-on: ubuntu-latest
    needs: [upload_files]
    outputs:
      email_body: ${{ steps.build-string.outputs.email_body }}
    steps:
      - name: Build string
        id: build-string
        run: |
          BODY=""
          for PLATFORM in StandaloneWindows64
          do
            BODY="$BODY\n$PLATFORM Download link https://github.com/${{ github.repository }}/releases/download/${{ env.RELEASE_NAME }}/Build-${PLATFORM}.zip"
          done
          # Expose BODY as an output
          echo "email_body<<EOF" >> $GITHUB_OUTPUT
          echo -e "$BODY" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
  send_email:
    name: Send email to ${{ matrix.emails }}
    runs-on: ubuntu-latest
    needs: [generate_email_body]
    strategy:
      fail-fast: false
      matrix:
        emails: [amandapnovelli3825@gmail.com, luccacatalan@gmail.com, lugo.bhz@gmail.com, rodrigomjcoelho@gmail.com, arthur.roque.pro@gmail.com]
        
    steps:
      
      - name: Send Email with Release Link
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "New Release"
          to: ${{ matrix.emails }}
          from: ${{ secrets.EMAIL_USERNAME }}
          body: ${{ needs.generate_email_body.outputs.email_body }}
